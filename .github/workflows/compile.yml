name: Build Arduino Project

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v2

      - name: Install ESP32 Core (2.0.17)
        run: |
          # Adiciona a URL do índice de placas
          arduino-cli core update-index --additional-urls https://espressif.github.io/arduino-esp32/package_esp32_index.json
          
          # Instala a versão específica 2.0.17 conforme solicitado
          arduino-cli core install esp32:esp32@2.0.17 --additional-urls https://espressif.github.io/arduino-esp32/package_esp32_index.json

      - name: Install Libraries
        run: |
          mkdir -p "$HOME/Arduino/libraries"
          
          # Instalação das bibliotecas necessárias
          arduino-cli lib install "Adafruit SSD1306"
          arduino-cli lib install "Adafruit GFX Library"
          arduino-cli lib install "U8g2_for_Adafruit_GFX"
          arduino-cli lib install "Adafruit NeoPixel"
          arduino-cli lib install "U8g2"
          arduino-cli lib install "RF24"
          
          # Copia a biblioteca local se ela existir no repositório
          if [ -d "SmartRC-CC1101-Driver-Lib2" ]; then
            cp -r SmartRC-CC1101-Driver-Lib2 "$HOME/Arduino/libraries/"
          fi

      - name: Compile Project
        run: |
          mkdir -p build
          
          # Comando de compilação com a flag de permissão de múltiplas definições
          arduino-cli compile --fqbn esp32:esp32:esp32 \
            --build-property "compiler.c.elf.extra_flags=-Wl,--allow-multiple-definition" \
            --build-path ./build \
            ./starbeam_v2/starbeam_v2.ino
            
