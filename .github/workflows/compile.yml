name: Build Arduino Project

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v2

      - name: Install ESP32 Core
        run: |
          arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Install Libraries
        run: |
          mkdir -p "$HOME/Arduino/libraries"
          # Instalação das bibliotecas necessárias via gerenciador
          arduino-cli lib install "Adafruit SSD1306"
          arduino-cli lib install "Adafruit GFX Library"
          arduino-cli lib install "U8g2_for_Adafruit_GFX"
          
          # Copia a biblioteca local SmartRC-CC1101-Driver-Lib2 se ela estiver na raiz do seu repositório
          if [ -d "SmartRC-CC1101-Driver-Lib2" ]; then
            cp -r SmartRC-CC1101-Driver-Lib2 "$HOME/Arduino/libraries/"
          fi

      - name: Compile Project
        run: |
          mkdir -p build
          # Compilação direcionada para o seu arquivo .ino
          arduino-cli compile --fqbn esp32:esp32:esp32:PartitionScheme=huge_app --build-path ./build ./starbeam_v2/starbeam_v2.ino
          
